## Tailwind CSS

To build the CSS for production, run the following command:

```
npm run build
```

For Netlify/Vercel, you can set the build command to `npm run build` and the publish directory to the root of the project.
